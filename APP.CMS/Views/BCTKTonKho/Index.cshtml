@using Portal.Utils;
@{
    ViewData["Title"] = "Báo cáo tồn kho";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header border-0">
                <div class="row" id="frmFilter">
                    <div class="form-group col-sm-4 col-md-4 col-12">
                        <input id="txtName" type="text" placeholder="Tên phụ tùng" class="form-control" />
                    </div>
                    <div class="col-sm-4 col-md-4 col-6">
                        <button id="btnSearch" class="btn btn-primary">Tìm kiếm</button>
                    </div>
                    <div class="col-sm-4 col-md-4 col-6">
                        @*@{
                            if ((int)ViewData[nameof(RolesEnum.Create)] == 1)
                            {*@
                        <button id="btnExport" class="btn btn-success float-right">Xuất exel</button>
                        @*}
                            }*@
                    </div>
                </div>
            </div>
            <!-- /.card-header -->
            <div id="dtTable">
            </div>
        </div>
    </div>
</div>
<script src="~/js/BCTonKho/BCTonkho.js"></script>

