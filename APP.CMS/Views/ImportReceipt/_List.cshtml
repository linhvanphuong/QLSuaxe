@model List<APP.MODELS.ImportReceipt>
@using Portal.Utils
<table style="width:100%;height:100%;" id="tblDisplay" class=" table table-bordered table-hover table-striped">
    <thead>
        <tr>
            <th style="text-align:center;width:10%;">Mã</th>
            <th style="width:60%">Nhà cung cấp</th>
            <th style="text-align:center;width:20%">Ngày tạo</th>
            <th style="text-align:center;width:10%">Thao tác</th>
        </tr>
    </thead>
    <tbody>
        @for (int i = 0; i < Model.Count; i++)
        //@foreach (var item in Model)
        {
            var item = Model[i];
            <tr id="row_@item.Id">
                <td style="text-align:center;width:10%;">@item.Id</td>
                <td style="width:60%">@item.SupllierName</td>
                <td style="text-align:center;width:20%">@item.CreatedDate.ToString("dd/MM/yyyy HH:mm:ss")</td>
                <td style="text-align:center;width:10%;">
                    @*@if (@item.Status == (byte)StatusEnum.Removed)
                        {
                            <a href="javascipt:;" class="btn-delete" onclick="restorn(@item.Id)"><i class="fas fa-trash-restore-alt"></i></a>
                        }
                        else
                        {
                            <a href="javascipt:;" class="btn-delete" onclick="deleteItem(@item.Id)"><i class="fas fa-trash-alt"></i></a>
                            <a class="btn-update" href="javascipt:;" onclick="openUpdate(@item.Id)"><i class="fas fa-edit"></i></a>
                        }*@
                    <a class="btn-update" href="/don-hang-nhap/xem?id=@item.Id"><i class="fas fa-eye"></i></a>

                </td>
            </tr>
        }
    </tbody>
</table>
<script src="~/js/ImportReceipt/List.js"></script>